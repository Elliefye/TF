<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TF2.BottomNavigation"
             xmlns:views="clr-namespace:TF2" 
             BarBackgroundColor="Transparent"
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
             android:TabbedPage.ToolbarPlacement ="Bottom"
             android:TabbedPage.BarItemColor="#999999"
             android:TabbedPage.BarSelectedItemColor="Black"
            >
    <!--Pages can be added as references or inline-->
    <TabbedPage.Children>
        <NavigationPage Title="Home" Icon="home_icon.png">
            <x:Arguments>
                <views:ViewAll />
            </x:Arguments>
        </NavigationPage>
        <NavigationPage Title="Reviews" Icon="review_icon.png">
            <x:Arguments>
                <views:Reviews />
            </x:Arguments>
        </NavigationPage>
        <NavigationPage Title="Profile" Icon="profile_icon.png">
            <x:Arguments>
                <views:Profile />
            </x:Arguments>
        </NavigationPage>
        <NavigationPage Title="Other" Icon="other_icon.png">
            <x:Arguments>
                <views:ViewAll />
            </x:Arguments>
        </NavigationPage>
    </TabbedPage.Children>
</TabbedPage>